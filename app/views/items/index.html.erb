<p style="color: green"><%= notice %></p>

<h1>Wszystko</h1>
<!---
<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", item %>
    </p>
  <% end %>
</div> --->
<!-- przycisk dodaj nowego
<%= link_to "New item", new_item_path, class:"btn btn-info" %> --->
<br><br>
<!--- lista rekordów  
<% @items.each do |item| %>
<div class="container">
<div class="row">
  <div class="card" style="border-radius:25px; border-bottom:1rem solid grey; margin-bottom:10px">
   <%= image_tag "1.jpg", weight:"175px", height:"175px"%>
   <h2> <%= item.name %> </h2>
   <p> <%= item.body%> </p>
   <div class="buttom">
   <%= link_to "Show this item", item, class:"btn btn-info" %> <P>
   </div>
  </div>
</div>
</div>
<% end %>
--->

<% (1..12).each_slice (12) do |item| %>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-lg-4" style="border-radius:25px;">

        <% if item.cover_image.attached?%>
          <%= image_tag item.cover_image, height:"280px",width:"100%", style:"border-radius:25px"%>
          <% else %>
         <%= image_tag "1.jpg", height:"185px",width:"185px", style:"border-radius:25px"%>
         <% end %>

         <div class="text-left">
            <h1 strong class="card-text"> <%= item.name %> </h1> </strong> 
          </div>
            <!--
            <p class="card-text"><%= item.body %></p> -->
            
            <%= item.item_type %>

            <div class="buttom">
              <%= link_to "Więcej", item, class:"btn btn-dark" %> <P>
            </div>
            
      </div>
      
    <% end %>
  </div>

</div>
<% end %>
